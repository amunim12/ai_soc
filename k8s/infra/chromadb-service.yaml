apiVersion: v1
kind: Service
metadata:
  name: chromadb
  namespace: ai-soc
  labels:
    app: chromadb
spec:
  clusterIP: None
  selector:
    app: chromadb
  ports:
    - name: http
      port: 8888
      targetPort: 8000
