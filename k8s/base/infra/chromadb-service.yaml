apiVersion: v1
kind: Service
metadata:
  name: chromadb
  labels:
    app: chromadb
spec:
  clusterIP: None
  selector:
    app: chromadb
  ports:
    - name: http
      port: 8888
      targetPort: 8000
